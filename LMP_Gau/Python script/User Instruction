Use the normal (without "mm") script for ReaxFF. If you are using a non-reactive force field, use the *_mm version of the script. The reason is that the LAMMPS data file format differs for the ReaxFF field from that for other standard molecular mechanics force fields.

The scripts are written by Enia Léon Mudimu and Indu Sekhar Roy